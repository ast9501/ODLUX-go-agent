apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-odlux-agent.fullname" . }}-test-connection"
  labels:
    {{- include "helm-odlux-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-odlux-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
